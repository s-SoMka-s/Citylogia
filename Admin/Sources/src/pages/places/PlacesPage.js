import React, { Component } from 'react'
import PropTypes from 'prop-types'

export class PlacesPage extends Component {
    static propTypes = {}

    render() {
        return (
            <div className="places-page">
                <h1>Places page works!</h1>
            </div>
        )
    }
}

export default PlacesPage
