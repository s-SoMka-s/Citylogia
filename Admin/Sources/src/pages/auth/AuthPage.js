import React, { Component } from 'react'
import PropTypes from 'prop-types'

export class AuthPage extends Component {
    static propTypes = {}

    render() {
        return (
            <div className="auth-page">
                <h1>Auth page works</h1>
            </div>
        )
    }
}

export default AuthPage
